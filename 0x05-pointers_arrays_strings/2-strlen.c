#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_strlen - return size os array's String
 *
 *@s: array of typ char
 *Return: size of array of char type
 */
int _strlen(char *s)
{
	return (strlen(s));
}
