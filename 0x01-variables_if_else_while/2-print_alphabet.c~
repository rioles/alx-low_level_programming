/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	int lastDigit;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	lastDigit = n % 10;

	lastDigitGreaterOrLess(lastDigit, n);
	/* your code goes there */
	return (0);
}
