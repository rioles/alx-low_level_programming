#include <stdio.h>
#include "main.h"


/**
 * add - Return sum of two numbers
 * and check the number's sign
 *
 *@a: first value
 *@b: second value
 * Return: 1 if lowercase 0 otherwise(Success)
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
