#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 * print_alphabet - Print alphabet
 *
 * Return: Always 0 (Success)
 */
 int _islower(int value)
 {
 	if (islower(value))
 	{
 		return 1;
 	}
 	else
 	{
 		return 0;

 	}
 }
